tasks:
  - name: aws-cli
    before:
      # Windows users install AWS CLI manually
      # - curl "https://awscli.amazonaws.com/awscli-exe-linux-x86_64.zip" -o "awscliv2.zip"
      # - unzip awscliv2.zip
      # - sudo ./aws/install
